import sys
import re

def parse_runtime(log_path):
    # TODO: Read log file, find CLR version (e.g. '.NET 8.0.0') and Host type (e.g. 'Kestrel' or 'IIS')
    # Return a dictionary with 'version' and 'host' keys
    return {'version': 'UNKNOWN', 'host': 'UNKNOWN'}
