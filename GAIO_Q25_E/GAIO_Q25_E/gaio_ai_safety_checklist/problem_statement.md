# GenAI Ops: AI Safety Checklist & Grounding Score Calculator

Duration : 45 Min.

## Scenario
Build a Python evaluation helper ('evaluate_safety.py') that checks an AI response against 6 evaluation dimensions and calculates a composite safety score.

## Target File Location & Creation
**File to Create/Update**: `student_workspace/evaluate_safety.py`
**Input Resource File to Inspect**: `student_workspace/ai_incident_resolution.txt`

## Task Objectives
Perform the following actions inside the `student_workspace` directory:
- Inspect `ai_incident_resolution.txt` in `student_workspace/`.
- Create or update `student_workspace/evaluate_safety.py` and populate it with valid analysis results.

## Instructions to Perform the Task
1. When your workspace loads in **VS Code**, use the **Explorer** panel on the left to locate your files.
2. Open and inspect `ai_incident_resolution.txt` inside `student_workspace/`.
3. Create or open `student_workspace/evaluate_safety.py` in `student_workspace/` and record your answers.
4. Save your changes (`Ctrl + S` or `Cmd + S`).
5. Open the built-in terminal by clicking **Terminal > New Terminal** from the top menu.
6. Verify your progress by running `python run.py` locally in the terminal.

## Validation
Once you have saved your files and verified your progress, return to the platform dashboard and click the **"Run Test" / "Verify"** button. This will automatically evaluate your changes and generate your score!

Your performance will be evaluated based on the following test cases:

| Test Case | Requirement | Marks |
| --------- | ----------- | ----- |
| **TC1** | evaluate_safety.py is syntax-valid Python | 3 Marks |
| **TC2** | Evaluates Accuracy, Grounding, Completeness, Format, Actionability, Safety | 3 Marks |
| **TC3** | Calculates composite safety score out of 100 | 2 Marks |
| **TC4** | Outputs safety_score.json | 2 Marks |

**Total Score: 10 Marks**

## Important Notes
- This is an auto-evaluated question. Ensure all code edits are properly saved and the 'run.py' checks pass before submission.

## Important Notes
- This is an auto-evaluated question. Ensure all code edits are properly saved and the 'run.py' checks pass before submission.
